name: Build autumn seasonal artworks

on:
  push:
    branches:
      - feat/autumn-seasonal-artworks
    paths:
      - scripts/build-autumn-artworks.mjs
      - scripts/add-autumn-full-preview.mjs
      - scripts/fix-chase-postcard.mjs
      - scripts/fix-postcard-ratio.mjs
      - .github/workflows/build-autumn-artworks.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-artworks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/autumn-seasonal-artworks
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci --ignore-scripts
      - run: node scripts/build-autumn-artworks.mjs
      - run: node scripts/add-autumn-full-preview.mjs
      - run: node scripts/fix-chase-postcard.mjs
      - run: node scripts/fix-postcard-ratio.mjs
      - name: Commit generated autumn preview
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/images/seasonal/*.webp lib/seasonal-artwork.ts app/components/DailyPostcard.tsx app/globals.css
          if git diff --cached --quiet; then
            echo "No generated preview changes"
            exit 0
          fi
          git commit -m "fix: preserve postcard 3:2 ratio"
          git push origin HEAD:feat/autumn-seasonal-artworks
