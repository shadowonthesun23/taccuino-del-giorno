# Asset legacy non pubblicati

Questi file erano presenti in `public/images`, ma non risultano più referenziati
dal codice attuale. Sono stati spostati qui per evitare che vengano pubblicati
in ogni nuovo deployment, mantenendoli comunque recuperabili dal repository.

Verifica eseguita il 6 settembre 2026 con ricerca dei riferimenti in `app/`,
`components/` e `lib/`. Non sono state spostate le immagini stagionali né gli
asset ancora usati dall'interfaccia o dagli export.
