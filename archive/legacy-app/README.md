# Archivio storico

Questi file sono snapshot di lavoro precedenti, conservati per riferimento.
Non fanno parte dell'applicazione Next.js, non vengono compilati e non devono essere modificati durante il lavoro ordinario.

Il backup locale non tracciato `app/page.tsx.bak` resta intenzionalmente fuori da questo archivio: appartiene al workspace dell'utente e non viene gestito dal repository.
